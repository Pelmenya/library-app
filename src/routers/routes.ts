export enum UserRoutes {
    USER = '/api/user',
    LOGIN = '/login',
}

export enum BooksRoutes {
    BOOKS = '/api/books',
    ID = '/:id',
    DOWNLOAD = '/download',
}

export enum ViewRoutes {
    INDEX = '/index',
    VIEW = '/view',
    CREATE = '/create',
    UPDATE = '/update',
}