export enum UserRoutes {
    USER = '/api/user',
    LOGIN = '/login',
}

export enum BooksRoutes {
    BOOKS = '/api/books',
    ID = '/:id',
    DOWNLOAD = '/download',
}

export enum ViewRoutes {
    BASE_URL = '/', 
    INDEX = '/index',
    VIEW = '/view',
    CREATE = '/create',
    UPDATE = '/update',
    NOT_FOUND_404 = '/not-found-404', 
}